# Last matching pattern wins. Deliberately no catch-all: an unowned path needs
# no code-owner approval, which is what lets the release bot merge its own
# appVersion bumps. Ownership here is a gate, so only gate what needs gating.

# CI/CD workflows and promotion tooling.
/.github/ @jdwillmsen

# Production-facing paths require an explicit human (code owner) review
# before a pull request touching them can merge. Enforced by the
# "PRD Promotion Review Gate" repository ruleset (require_code_owner_review).
/charts/*/values-prd.yaml @jdwillmsen
/argocd/prd/ @jdwillmsen

# (probe: verifying code-owner enforcement with required_approving_review_count=0)
