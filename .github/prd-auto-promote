# Charts (directory names under charts/) enabled for automatic prd promotion
# pull requests after a passing E2E run against non. One name per line.
#
# Empty, but not as a block: the E2E run that gates this allowlist is
# manual-only while its runner is dormant, so no entry here would be acted on
# yet - promotion goes through workflow_dispatch in the meantime. Add a chart
# once that trigger chain is live again, and for a chart whose prd pin sits a
# major version behind its appVersion, only after the cross-generation check
# in docs/prd-promotion.md.
